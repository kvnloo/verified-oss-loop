# Security

This repository is a protocol and a set of shell templates. It does not spawn agents or hold tokens.

- Do not open issues with secrets, pairing codes, or private transcripts.
- Report vulnerabilities privately to the maintainer (`kvnloo`).
- Templates must not grant workers merge authority or secret access.
- `--install` in `scripts/setup-verify.sh` only runs when the operator passes that flag.
